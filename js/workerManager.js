//This file is for handling data that comes from the web worker.

function analyzeWorker(received){

/*switch (data.cmd) {
    case 'done':
      console.log('WORKER done: ' + data.msg);
      app.showAlert("Time Elapsed", "Super Title");
	w = undefined;

	break;
	
	case 'continue':
      console.log(data.msg);

	break;

};
}, false);
*/
}
